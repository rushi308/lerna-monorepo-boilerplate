{
  "name": "root",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "start": "lerna run start",
    "clean": "lerna clean -y",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^5.0.0"
  }
}
